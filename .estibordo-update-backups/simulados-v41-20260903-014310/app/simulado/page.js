import {redirect} from "next/navigation";import {getSession} from "../../lib/auth";import {getUserAccess} from "../../lib/access";import {query} from "../../lib/db";import {SUBJECTS,subjectLabel} from "../../lib/subjects";import StudentHeader from "../components/StudentHeader";import styles from "./simulados.module.css";export default async function Page(){const s=await getSession();if(!s)redirect('/login');if(!(await getUserAccess(s.id))?.active)redirect('/comprar');const r=await query('select * from exam_sessions where user_id=$1 order by started_at desc',[s.id]);return <><StudentHeader active="simulados"/><main className={styles.page}><span>PROVAS</span><h1>Simulados</h1><section className={styles.launch}>{SUBJECTS.map(x=><a key={x.slug} href={`/simulado/${x.slug}`}><b>{x.label}</b><small>100 questões · 240 minutos</small></a>)}</section><h2>Todos os simulados lançados</h2><div className={styles.history}>{r.rows.length?r.rows.map(x=><a href={`/simulado/tentativa/${x.id}`} key={x.id}><div><b>{subjectLabel(x.subject)}</b><small>{new Date(x.started_at).toLocaleString('pt-BR')} · {x.status}</small></div><strong>{x.correct_count||0}/{x.answered_count||0}</strong><span>Acessar →</span></a>):<p>Nenhum simulado ainda.</p>}</div></main></>}