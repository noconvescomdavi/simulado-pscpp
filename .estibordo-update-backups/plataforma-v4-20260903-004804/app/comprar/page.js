import {redirect} from "next/navigation";import {getSession} from "../../lib/auth";export default async function Comprar(){const s=await getSession();if(!s)redirect("/cadastro");return <main className="wrap"><section className="auth"><div className="eyebrow">ACESSO VITALÍCIO</div><h1>Conta criada</h1><p className="muted">O gateway de pagamento será conectado posteriormente. A estrutura de licença já está pronta no PostgreSQL.</p><p>Quando o pagamento for integrado, o webhook deverá alterar o acesso para <b>active</b>. Durante os testes, você pode ativar uma conta diretamente pelo SQL.</p><a className="btn primary" href="/area-do-aluno">Área do aluno</a></section></main>}