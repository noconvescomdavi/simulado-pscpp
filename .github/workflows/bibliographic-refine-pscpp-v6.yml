name: Retired — bibliographic-refine-pscpp-v6

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy pipeline retired
        run: |
          echo "Pipeline legado desativado pela auditoria integral PSCPP."
          echo "Não é permitido sintetizar ou reescrever questões por composição automática."
          echo "Use revisão individual baseada na bibliografia e nas provas históricas."
