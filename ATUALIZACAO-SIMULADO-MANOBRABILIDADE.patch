diff --git a/public/study-content/simulado/manobrabilidade/index.html b/public/study-content/simulado/manobrabilidade/index.html
index 2aedfcc..00ecdcb 100644
--- a/public/study-content/simulado/manobrabilidade/index.html
+++ b/public/study-content/simulado/manobrabilidade/index.html
@@ -1,32 +1,19 @@
 <!doctype html><html lang="pt-BR"><head><meta charset="utf-8">
     <meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><meta name="theme-color" content="#061827">
-    <title>I – MANOBRABILIDADE DO NAVIO (SHIP MANOEUVERABILITY) | SIMULADOS PSCPP</title><link rel="stylesheet" href="../../assets/css/style.css"><link rel="stylesheet" href="./questions.css"><script src="../../assets/js/platform-config.js"></script><script type="module" src="../../assets/js/guard.js"></script></head>
-    <body data-subject="manobrabilidade"><header><div class="header-in"><a class="brand" href="../"><div class="logo">⚓</div><div><b>SIMULADOS PSCPP</b><small>@noconvescomdavi</small></div></a>
+    <title>I – MANOBRABILIDADE DO NAVIO (SHIP MANOEUVERABILITY) | SIMULADOS PSCPP</title><link rel="stylesheet" href="/assets/css/style.css"><link rel="stylesheet" href="/study-content/simulado/manobrabilidade/questions.css"><script src="/assets/js/platform-config.js"></script><script type="module" src="/assets/js/guard.js"></script></head>
+    <body data-subject="manobrabilidade"><header><div class="header-in"><a class="brand" href="/simulado"><div class="logo">⚓</div><div><b>SIMULADOS PSCPP</b><small>@noconvescomdavi</small></div></a>
     <div class="head-actions"><span class="sig">@noconvescomdavi</span><button id="themeBtn" class="iconbtn">🌙</button></div></div></header>
     <main class="shell"><nav class="breadcrumb"><a href="../">SIMULADOS PSCPP</a><span>›</span><span>I – MANOBRABILIDADE DO NAVIO (SHIP MANOEUVERABILITY)</span></nav>
     <section class="subject-hero"><span class="subject-kicker">DISCIPLINA I</span><h1>⚓ MANOBRABILIDADE DO NAVIO (SHIP MANOEUVERABILITY)</h1>
     <div class="subtitle">SHIP MANOEUVRABILITY</div>
     <p>Conteúdo programático detalhado da disciplina. Marque os itens estudados; o progresso fica salvo neste navegador.</p>
     <div class="progress-row"><div class="progress-track"><div class="progress-fill"></div></div><div class="progress-text">0%</div></div></section>
-    <section class="qb-shell" id="questionBank" aria-labelledby="qbTitle">
-      <div class="qb-head"><div><span class="subject-kicker">BANCO DE QUESTÕES</span><h2 id="qbTitle">Simulado de Manobrabilidade</h2><p>300 questões com correção imediata, justificativa e fonte bibliográfica.</p></div><div class="qb-total"><strong id="qbTotal">300</strong><span>questões disponíveis</span></div></div>
-      <div class="qb-message" id="qbLoading">Carregando o banco de questões...</div>
-      <div class="qb-message qb-error hidden" id="qbError"></div>
-      <div class="qb-setup hidden" id="qbSetup">
-        <label>Conteúdo<select id="qbModule"><option value="all">Todos os módulos</option></select></label>
-        <label>Quantidade<select id="qbAmount"><option value="20">20 questões</option><option value="50">50 questões</option><option value="100">100 questões</option><option value="300">Banco completo</option></select></label>
-        <button class="qb-primary" id="qbStart" type="button">Iniciar simulado</button>
+    <section class="qb-shell qb-launch-card" id="questionBank" aria-labelledby="qbTitle">
+      <div class="qb-head"><div><span class="subject-kicker">BANCO DE QUESTÕES</span><h2 id="qbTitle">Simulado de Manobrabilidade</h2><p>300 questões com correção imediata, justificativa e fonte bibliográfica.</p></div><div class="qb-total"><strong>300</strong><span>questões disponíveis</span></div></div>
+      <div class="qb-launch">
+        <div class="qb-launch-copy"><span class="subject-kicker">PROVA INTERATIVA</span><h3>100 questões aleatórias. Uma resposta por vez.</h3><p>Você terá até 3 horas. Selecione uma alternativa, salve a resposta para receber a correção e avance para a próxima questão.</p><div class="qb-launch-details"><span>⏱ 3 horas</span><span>✓ Correção comentada</span><span>↗ Métricas automáticas</span></div></div>
+        <a class="qb-primary qb-launch-button" href="/simulado/manobrabilidade?novo=1">Iniciar Novo Simulado <span aria-hidden="true">→</span></a>
       </div>
-      <div class="qb-exam hidden" id="qbExam">
-        <div class="qb-status"><span id="qbPosition">Questão 1 de 20</span><span id="qbScore">0 acertos</span></div>
-        <div class="qb-progress"><i id="qbProgress"></i></div>
-        <div class="qb-meta"><span id="qbId"></span><span id="qbTopic"></span></div>
-        <h3 id="qbQuestion"></h3>
-        <div class="qb-options" id="qbOptions"></div>
-        <div class="qb-feedback hidden" id="qbFeedback"><strong id="qbFeedbackTitle"></strong><p id="qbExplanation"></p><small id="qbSource"></small></div>
-        <div class="qb-actions"><button class="qb-secondary" id="qbStop" type="button">Encerrar</button><div class="qb-answer-actions"><button class="qb-primary" id="qbSave" type="button" disabled>Salvar resposta</button><button class="qb-primary hidden" id="qbNext" type="button">Próxima questão</button></div></div>
-      </div>
-      <div class="qb-result hidden" id="qbResult"><span class="subject-kicker">RESULTADO</span><strong id="qbResultScore">0%</strong><p id="qbResultText"></p><button class="qb-primary" id="qbRestart" type="button">Fazer outro simulado</button></div>
     </section>
     <div class="tools"><div class="toolbar"><input id="topicSearch" class="search" placeholder="Buscar tópico, termo, norma, equipamento...">
     <button id="expandAll" class="toolbtn">Expandir tudo</button><button id="collapseAll" class="toolbtn">Recolher</button></div></div>
@@ -925,4 +912,4 @@
     <div id="noResults" class="no-results hidden">Nenhum tópico encontrado para esta busca.</div>
     <div class="signature-banner"><span>SIMULADOS PSCPP · Disciplina I</span><b>@noconvescomdavi</b></div></main>
     <footer><span>MANOBRABILIDADE DO NAVIO (SHIP MANOEUVERABILITY)</span><span>Conteúdo de estudo · <b>@noconvescomdavi</b></span></footer>
-    <script src="../../assets/js/app.js"></script><script type="module" src="./questions.js"></script></body></html>
+    <script src="/assets/js/app.js"></script></body></html>
diff --git a/public/study-content/simulado/manobrabilidade/questions.css b/public/study-content/simulado/manobrabilidade/questions.css
index ecf14ae..cf244f3 100644
--- a/public/study-content/simulado/manobrabilidade/questions.css
+++ b/public/study-content/simulado/manobrabilidade/questions.css
@@ -8,4 +8,6 @@
 .qb-option.selected{border-color:var(--blue);box-shadow:0 0 0 2px color-mix(in srgb,var(--blue) 18%,transparent);background:color-mix(in srgb,var(--blue) 8%,var(--surface))}.qb-option.selected b{background:var(--blue);border-color:var(--blue);color:#fff}
 .qb-feedback{margin-top:15px;border:1px solid var(--line);border-left:4px solid var(--blue);border-radius:13px;padding:14px 15px;background:var(--surface2)}.qb-feedback.correct{border-left-color:var(--green)}.qb-feedback.wrong{border-left-color:var(--red)}.qb-feedback strong{display:block;margin-bottom:5px}.qb-feedback p{margin:0;line-height:1.55;font-size:13px}.qb-feedback small{display:block;color:var(--muted);line-height:1.45;margin-top:8px}.qb-actions{margin-top:15px}.qb-answer-actions{display:flex;gap:9px;margin-left:auto}
 .qb-result{margin-top:22px;text-align:center;border:1px solid var(--line);background:var(--surface2);border-radius:17px;padding:25px}.qb-result>strong{display:block;font-size:52px;color:var(--blue);margin:8px 0}.qb-result p{color:var(--muted);line-height:1.55}
+.qb-launch{margin-top:22px;border:1px solid var(--line);background:linear-gradient(135deg,var(--navy),var(--navy2));color:#fff;border-radius:18px;padding:clamp(20px,4vw,30px);display:grid;grid-template-columns:1fr auto;gap:28px;align-items:center;position:relative;overflow:hidden}.qb-launch:after{content:"";position:absolute;width:240px;height:240px;border-radius:50%;right:-110px;top:-145px;background:radial-gradient(circle,#17a1c548,transparent 68%);pointer-events:none}.qb-launch-copy,.qb-launch-button{position:relative;z-index:1}.qb-launch h3{font-size:clamp(21px,3vw,30px);margin:7px 0 8px;line-height:1.2}.qb-launch p{margin:0;max-width:760px;color:#c7d8e5;line-height:1.6;font-size:13px}.qb-launch-details{display:flex;gap:8px;flex-wrap:wrap;margin-top:17px}.qb-launch-details span{border:1px solid #ffffff24;background:#ffffff0d;border-radius:999px;padding:7px 10px;font-size:10px;font-weight:850}.qb-launch-button{display:inline-flex;align-items:center;justify-content:center;gap:12px;min-height:52px;padding:0 20px;white-space:nowrap;background:#c8102e;border-color:#c8102e;box-shadow:0 13px 28px rgba(200,16,46,.28)}.qb-launch-button:hover{transform:translateY(-2px);background:#e11b3b}
+@media(max-width:820px){.qb-launch{grid-template-columns:1fr}.qb-launch-button{width:100%}}
 @media(max-width:720px){.qb-head{align-items:flex-start}.qb-total{display:none}.qb-setup{grid-template-columns:1fr}.qb-option{grid-template-columns:30px 1fr;padding:11px}.qb-shell{border-radius:18px}.qb-actions>*{flex:1}.qb-answer-actions{width:100%;margin-left:0}.qb-answer-actions>*{flex:1}}
diff --git a/app/simulado/manobrabilidade/page.js b/app/simulado/manobrabilidade/page.js
new file mode 100644
index 0000000..da33981
--- /dev/null
+++ b/app/simulado/manobrabilidade/page.js
@@ -0,0 +1,22 @@
+import { redirect } from "next/navigation";
+import { getSession } from "../../../lib/auth";
+import { query } from "../../../lib/db";
+import SimuladoClient from "./SimuladoClient";
+
+export const metadata = {
+  title: "Simulado de Manobrabilidade",
+  description: "Prova interativa de Manobrabilidade com 100 questões aleatórias e correção comentada.",
+};
+
+export default async function SimuladoManobrabilidade() {
+  const session = await getSession();
+  if (!session) redirect("/login?next=/simulado/manobrabilidade");
+
+  const access = await query(
+    "select status from user_access where user_id=$1 and product_code='pscpp-vitalicio'",
+    [session.id]
+  );
+  if (access.rows[0]?.status !== "active") redirect("/comprar?locked=1");
+
+  return <SimuladoClient userEmail={session.email} />;
+}
diff --git a/app/simulado/manobrabilidade/SimuladoClient.js b/app/simulado/manobrabilidade/SimuladoClient.js
new file mode 100644
index 0000000..40c74a2
--- /dev/null
+++ b/app/simulado/manobrabilidade/SimuladoClient.js
@@ -0,0 +1,399 @@
+"use client";
+
+import { useEffect, useMemo, useRef, useState } from "react";
+import styles from "./simulado.module.css";
+
+const SUBJECT = "manobrabilidade";
+const EXAM_SIZE = 100;
+const EXAM_DURATION_MS = 3 * 60 * 60 * 1000;
+const STORAGE_KEY = "estibordo:simulado:manobrabilidade:v3";
+
+function shuffle(items) {
+  const result = [...items];
+  for (let index = result.length - 1; index > 0; index -= 1) {
+    const randomIndex = Math.floor(Math.random() * (index + 1));
+    [result[index], result[randomIndex]] = [result[randomIndex], result[index]];
+  }
+  return result;
+}
+
+function formatTime(totalSeconds) {
+  const safe = Math.max(0, Number(totalSeconds) || 0);
+  const hours = Math.floor(safe / 3600);
+  const minutes = Math.floor((safe % 3600) / 60);
+  const seconds = Math.floor(safe % 60);
+  return [hours, minutes, seconds].map((value) => String(value).padStart(2, "0")).join(":");
+}
+
+function sourceText(source = {}) {
+  return [source.author, source.title, source.edition, source.locator]
+    .filter(Boolean)
+    .join(". ");
+}
+
+function storedExam(snapshot) {
+  return {
+    version: 3,
+    ids: snapshot.questions.map((question) => question.id),
+    index: snapshot.index,
+    correct: snapshot.correct,
+    answered: snapshot.answered,
+    startedAt: snapshot.startedAt,
+    expiresAt: snapshot.expiresAt,
+    questionStartedAt: snapshot.questionStartedAt,
+    selected: snapshot.selected,
+    correction: snapshot.correction,
+  };
+}
+
+function persistExam(snapshot) {
+  localStorage.setItem(STORAGE_KEY, JSON.stringify(storedExam(snapshot)));
+}
+
+function clearStoredExam() {
+  localStorage.removeItem(STORAGE_KEY);
+}
+
+function createExam(bank) {
+  const now = Date.now();
+  return {
+    questions: shuffle(bank).slice(0, Math.min(EXAM_SIZE, bank.length)),
+    index: 0,
+    correct: 0,
+    answered: 0,
+    startedAt: now,
+    expiresAt: now + EXAM_DURATION_MS,
+    questionStartedAt: now,
+    selected: null,
+    correction: null,
+  };
+}
+
+function restoreExam(bank) {
+  try {
+    const saved = JSON.parse(localStorage.getItem(STORAGE_KEY) || "null");
+    if (!saved || saved.version !== 3 || !Array.isArray(saved.ids) || !saved.ids.length) return null;
+    const byId = new Map(bank.map((question) => [question.id, question]));
+    const questions = saved.ids.map((id) => byId.get(id)).filter(Boolean);
+    if (questions.length !== saved.ids.length) return null;
+    return {
+      questions,
+      index: Math.max(0, Math.min(Number(saved.index) || 0, questions.length - 1)),
+      correct: Math.max(0, Number(saved.correct) || 0),
+      answered: Math.max(0, Number(saved.answered) || 0),
+      startedAt: Number(saved.startedAt) || Date.now(),
+      expiresAt: Number(saved.expiresAt) || Date.now(),
+      questionStartedAt: Number(saved.questionStartedAt) || Date.now(),
+      selected: saved.selected || null,
+      correction: saved.correction || null,
+    };
+  } catch {
+    return null;
+  }
+}
+
+async function registerAttempt(snapshot) {
+  if (!snapshot.answered) return { ok: true };
+  const durationSeconds = Math.max(
+    1,
+    Math.min(3 * 60 * 60, Math.round((Date.now() - snapshot.startedAt) / 1000))
+  );
+  const response = await fetch("/api/attempts", {
+    method: "POST",
+    credentials: "same-origin",
+    headers: { "Content-Type": "application/json" },
+    body: JSON.stringify({
+      module: "Simulado de Manobrabilidade — 100 questões",
+      subject: SUBJECT,
+      correct_answers: snapshot.correct,
+      wrong_answers: snapshot.answered - snapshot.correct,
+      total_questions: snapshot.answered,
+      duration_seconds: durationSeconds,
+    }),
+  });
+  const payload = await response.json().catch(() => ({}));
+  if (!response.ok) throw new Error(payload.error || "Não foi possível registrar o resumo da prova.");
+  return payload;
+}
+
+export default function SimuladoClient({ userEmail }) {
+  const [theme, setTheme] = useState("light");
+  const [phase, setPhase] = useState("loading");
+  const [exam, setExam] = useState(null);
+  const [remaining, setRemaining] = useState(EXAM_DURATION_MS / 1000);
+  const [saving, setSaving] = useState(false);
+  const [error, setError] = useState("");
+  const [result, setResult] = useState(null);
+  const finishing = useRef(false);
+
+  useEffect(() => {
+    const savedTheme = localStorage.getItem("pscpp:theme");
+    const initialTheme = savedTheme === "dark" ? "dark" : "light";
+    setTheme(initialTheme);
+    document.documentElement.dataset.theme = initialTheme;
+  }, []);
+
+  useEffect(() => {
+    let alive = true;
+    async function boot() {
+      try {
+        const response = await fetch(`/api/questions/${SUBJECT}`, {
+          credentials: "same-origin",
+          cache: "no-store",
+        });
+        if (response.status === 401) {
+          location.href = `/login?next=${encodeURIComponent(location.pathname)}`;
+          return;
+        }
+        if (response.status === 403) {
+          location.href = "/comprar?locked=1";
+          return;
+        }
+        const payload = await response.json().catch(() => ({}));
+        if (!response.ok) throw new Error(payload.error || "Não foi possível carregar as questões.");
+        if (!Array.isArray(payload.questions) || !payload.questions.length) {
+          throw new Error("O banco de questões está vazio.");
+        }
+        if (!alive) return;
+
+        const wantsNewExam = new URLSearchParams(location.search).get("novo") === "1";
+        if (wantsNewExam) {
+          clearStoredExam();
+          history.replaceState(null, "", location.pathname);
+        }
+        let nextExam = wantsNewExam ? null : restoreExam(payload.questions);
+        if (nextExam && nextExam.expiresAt <= Date.now()) {
+          if (nextExam.answered) {
+            let message = "O limite de 3 horas foi atingido. Foram contabilizadas apenas as respostas salvas.";
+            try {
+              await registerAttempt(nextExam);
+            } catch (attemptError) {
+              message += ` ${attemptError.message}`;
+            }
+            if (!alive) return;
+            setResult({
+              correct: nextExam.correct,
+              answered: nextExam.answered,
+              reason: "timeout",
+              message,
+            });
+            clearStoredExam();
+            setPhase("result");
+            return;
+          }
+          clearStoredExam();
+          nextExam = null;
+        }
+
+        nextExam ||= createExam(payload.questions);
+        persistExam(nextExam);
+        setExam(nextExam);
+        setRemaining(Math.max(0, Math.ceil((nextExam.expiresAt - Date.now()) / 1000)));
+        setPhase("exam");
+      } catch (bootError) {
+        if (!alive) return;
+        setError(bootError.message || "Erro ao preparar o simulado.");
+        setPhase("error");
+      }
+    }
+    boot();
+    return () => {
+      alive = false;
+    };
+  }, []);
+
+  async function finishExam(reason = "completed", snapshot = exam) {
+    if (!snapshot || finishing.current) return;
+    if (reason === "manual") {
+      const confirmed = window.confirm(
+        `Encerrar agora? ${snapshot.answered} de ${snapshot.questions.length} questões respondidas serão contabilizadas.`
+      );
+      if (!confirmed) return;
+    }
+    finishing.current = true;
+    setSaving(true);
+    let message = reason === "timeout"
+      ? "O limite de 3 horas foi atingido. Foram contabilizadas apenas as respostas salvas."
+      : "Seu resultado foi registrado e as métricas da Área do Aluno foram atualizadas.";
+    try {
+      await registerAttempt(snapshot);
+    } catch (attemptError) {
+      message = `${message} ${attemptError.message}`;
+    }
+    clearStoredExam();
+    setResult({
+      correct: snapshot.correct,
+      answered: snapshot.answered,
+      reason,
+      message,
+    });
+    setPhase("result");
+    setSaving(false);
+  }
+
+  useEffect(() => {
+    if (phase !== "exam" || !exam) return undefined;
+    function tick() {
+      const seconds = Math.max(0, Math.ceil((exam.expiresAt - Date.now()) / 1000));
+      setRemaining(seconds);
+      if (seconds === 0) finishExam("timeout", exam);
+    }
+    tick();
+    const timer = window.setInterval(tick, 1000);
+    return () => window.clearInterval(timer);
+  }, [phase, exam]);
+
+  const question = exam?.questions[exam.index] || null;
+  const progress = exam ? Math.round((exam.answered / exam.questions.length) * 100) : 0;
+  const score = exam?.answered ? Math.round((exam.correct / exam.answered) * 100) : 0;
+  const resultScore = result?.answered ? Math.round((result.correct / result.answered) * 100) : 0;
+  const timerWarning = remaining <= 15 * 60;
+
+  function selectOption(key) {
+    if (!exam || exam.correction || saving) return;
+    const next = { ...exam, selected: key };
+    setExam(next);
+    persistExam(next);
+  }
+
+  async function saveAnswer() {
+    if (!exam?.selected || exam.correction || saving || !question) return;
+    setSaving(true);
+    setError("");
+    try {
+      const response = await fetch(`/api/questions/${SUBJECT}/answer`, {
+        method: "POST",
+        credentials: "same-origin",
+        headers: { "Content-Type": "application/json" },
+        body: JSON.stringify({
+          question_id: question.id,
+          selected_answer: exam.selected,
+          response_time_ms: Date.now() - exam.questionStartedAt,
+        }),
+      });
+      const payload = await response.json().catch(() => ({}));
+      if (!response.ok) throw new Error(payload.error || "Não foi possível salvar a resposta.");
+      const next = {
+        ...exam,
+        correct: exam.correct + (payload.is_correct ? 1 : 0),
+        answered: exam.answered + 1,
+        correction: payload,
+      };
+      setExam(next);
+      persistExam(next);
+    } catch (saveError) {
+      setError(saveError.message || "Erro ao salvar a resposta.");
+    } finally {
+      setSaving(false);
+    }
+  }
+
+  function nextQuestion() {
+    if (!exam?.correction) return;
+    if (exam.index >= exam.questions.length - 1) {
+      finishExam("completed", exam);
+      return;
+    }
+    const next = {
+      ...exam,
+      index: exam.index + 1,
+      selected: null,
+      correction: null,
+      questionStartedAt: Date.now(),
+    };
+    setExam(next);
+    persistExam(next);
+    setError("");
+    window.scrollTo({ top: 0, behavior: "smooth" });
+  }
+
+  function startAnotherExam() {
+    clearStoredExam();
+    location.reload();
+  }
+
+  function toggleTheme() {
+    const nextTheme = theme === "dark" ? "light" : "dark";
+    setTheme(nextTheme);
+    localStorage.setItem("pscpp:theme", nextTheme);
+    document.documentElement.dataset.theme = nextTheme;
+  }
+
+  const numberedQuestions = useMemo(
+    () => exam?.questions.map((item, index) => ({ id: item.id, number: index + 1 })) || [],
+    [exam?.questions]
+  );
+
+  if (phase === "loading") {
+    return <main className={styles.statePage} data-theme={theme}><div className={styles.spinner} /><h1>Preparando seu simulado...</h1><p>Sorteando 100 questões de Manobrabilidade.</p></main>;
+  }
+
+  if (phase === "error") {
+    return <main className={styles.statePage} data-theme={theme}><span className={styles.stateIcon}>!</span><h1>Não foi possível iniciar</h1><p>{error}</p><div className={styles.stateActions}><button onClick={() => location.reload()}>Tentar novamente</button><a href="/study-content/simulado/manobrabilidade">Voltar ao conteúdo</a></div></main>;
+  }
+
+  if (phase === "result" && result) {
+    return <main className={styles.resultPage} data-theme={theme}>
+      <a className={styles.resultBrand} href="/"><img src="/estibordo/logos/estibordo-logo-horizontal.svg" alt="ESTIBORDO" /></a>
+      <section className={styles.resultCard}>
+        <span className={styles.eyebrow}>SIMULADO CONCLUÍDO</span>
+        <div className={styles.resultGauge}><strong>{resultScore}%</strong><span>APROVEITAMENTO</span></div>
+        <h1>Resultado de Manobrabilidade</h1>
+        <div className={styles.resultStats}><div><span>RESPONDIDAS</span><strong>{result.answered}</strong></div><div><span>ACERTOS</span><strong className={styles.successText}>{result.correct}</strong></div><div><span>ERROS</span><strong className={styles.dangerText}>{result.answered - result.correct}</strong></div></div>
+        <p>{result.message}</p>
+        <div className={styles.resultActions}><a href="/area-do-aluno#desempenho">Ver meu desempenho</a><button onClick={startAnotherExam}>Iniciar Novo Simulado</button></div>
+      </section>
+    </main>;
+  }
+
+  return <div className={styles.page} data-theme={theme}>
+    <header className={styles.header}>
+      <div className={styles.headerInner}>
+        <a className={styles.brand} href="/"><img src="/estibordo/logos/estibordo-logo-horizontal.svg" alt="ESTIBORDO" /></a>
+        <div className={styles.headerMeta}><span>{userEmail}</span><a href="/area-do-aluno#desempenho">Desempenho</a><button type="button" onClick={toggleTheme} aria-label="Alternar tema">{theme === "dark" ? "☀️" : "🌙"}</button><form action="/api/auth/logout" method="post"><button type="submit">Sair</button></form></div>
+      </div>
+    </header>
+
+    <main className={styles.main}>
+      <div className={styles.examHeading}>
+        <div><span className={styles.eyebrow}>PROVA INTERATIVA · MANOBRABILIDADE</span><h1>Simulado de Manobrabilidade</h1><p>100 questões aleatórias · correção comentada · máximo de 3 horas</p></div>
+        <div className={`${styles.timer} ${timerWarning ? styles.timerWarning : ""}`}><span>Tempo restante</span><strong>{formatTime(remaining)}</strong></div>
+      </div>
+
+      <div className={styles.overallProgress}><i style={{ width: `${progress}%` }} /></div>
+      <div className={styles.progressLegend}><span>{exam.answered} de {exam.questions.length} respondidas</span><span>{progress}% concluído</span></div>
+
+      <div className={styles.examLayout}>
+        <section className={styles.questionCard}>
+          <div className={styles.questionTop}><div><span>QUESTÃO {exam.index + 1}</span><small>{question.id}</small></div><div className={styles.liveScore}><span>ACERTOS</span><strong>{exam.correct}</strong><small>{score}%</small></div></div>
+          <div className={styles.questionTags}><span>{question.module}</span><span>{question.topic_code} · {question.topic}</span></div>
+          <h2>{question.question}</h2>
+
+          <div className={styles.options}>
+            {question.options.map((option) => {
+              const isSelected = exam.selected === option.key;
+              const isCorrect = exam.correction?.correct_answer === option.key;
+              const isWrong = exam.correction && isSelected && !exam.correction.is_correct;
+              const className = [styles.option, isSelected && !exam.correction ? styles.optionSelected : "", isCorrect ? styles.optionCorrect : "", isWrong ? styles.optionWrong : ""].filter(Boolean).join(" ");
+              return <button className={className} type="button" key={option.key} disabled={Boolean(exam.correction) || saving} onClick={() => selectOption(option.key)}><b>{option.key}</b><span>{option.text}</span></button>;
+            })}
+          </div>
+
+          {error && <div className={styles.saveError}>{error}</div>}
+          {exam.correction && <article className={`${styles.feedback} ${exam.correction.is_correct ? styles.feedbackCorrect : styles.feedbackWrong}`}><strong>{exam.correction.is_correct ? `Você acertou. Gabarito: ${exam.correction.correct_answer}` : `Você errou. Gabarito: ${exam.correction.correct_answer}`}</strong><p>{exam.correction.explanation}</p><small>Fonte: {sourceText(exam.correction.source)}</small></article>}
+
+          <div className={styles.actions}>
+            <button className={styles.stopButton} type="button" disabled={saving} onClick={() => finishExam("manual", exam)}>Encerrar prova</button>
+            {!exam.correction ? <button className={styles.saveButton} type="button" disabled={!exam.selected || saving} onClick={saveAnswer}>{saving ? "Salvando..." : "Salvar resposta"}</button> : <button className={styles.nextButton} type="button" disabled={saving} onClick={nextQuestion}>{exam.index === exam.questions.length - 1 ? "Finalizar simulado" : "Próxima questão"}<span>→</span></button>}
+          </div>
+        </section>
+
+        <aside className={styles.sidebar}>
+          <section className={styles.sidebarCard}><span className={styles.sidebarLabel}>PROGRESSO DA PROVA</span><div className={styles.sidebarSummary}><div><strong>{exam.answered}</strong><span>respondidas</span></div><div><strong>{exam.correct}</strong><span>acertos</span></div><div><strong>{exam.answered - exam.correct}</strong><span>erros</span></div></div></section>
+          <section className={styles.sidebarCard}><div className={styles.navigatorHeading}><span className={styles.sidebarLabel}>QUESTÕES</span><small>Salve para avançar</small></div><div className={styles.questionGrid}>{numberedQuestions.map((item, index) => <span key={item.id} className={[styles.questionNumber, index < exam.index ? styles.questionAnswered : "", index === exam.index ? styles.questionCurrent : ""].filter(Boolean).join(" ")}>{item.number}</span>)}</div></section>
+          <a className={styles.backLink} href="/study-content/simulado/manobrabilidade">← Voltar ao conteúdo programático</a>
+        </aside>
+      </div>
+    </main>
+  </div>;
+}
diff --git a/app/simulado/manobrabilidade/simulado.module.css b/app/simulado/manobrabilidade/simulado.module.css
new file mode 100644
index 0000000..9b92ae1
--- /dev/null
+++ b/app/simulado/manobrabilidade/simulado.module.css
@@ -0,0 +1,26 @@
+.page,.statePage,.resultPage{--exam-navy:#071827;--exam-navy-2:#0d2c44;--exam-red:#c8102e;--exam-blue:#166ba5;--exam-ink:#102234;--exam-muted:#6a7b8b;--exam-line:#dbe4eb;--exam-bg:#edf3f7;--exam-surface:#fff;min-height:100vh;background:var(--exam-bg);color:var(--exam-ink)}
+.page[data-theme="dark"],.statePage[data-theme="dark"],.resultPage[data-theme="dark"]{--exam-ink:#edf4fa;--exam-muted:#a3b3c0;--exam-line:#284153;--exam-bg:#07121c;--exam-surface:#0d1f2d;--exam-blue:#65b9f1}
+.header{height:76px;background:rgba(7,24,39,.985);border-bottom:1px solid rgba(255,255,255,.09);position:sticky;top:0;z-index:50;backdrop-filter:blur(14px)}
+.headerInner{max-width:1320px;height:100%;margin:auto;padding:0 24px;display:flex;align-items:center;justify-content:space-between;gap:24px}
+.brand{width:248px;flex:0 0 auto}.brand img{width:100%;height:auto}
+.headerMeta{display:flex;align-items:center;gap:9px;color:#fff}.headerMeta>span{font-size:10px;color:#9fb0be;margin-right:4px}.headerMeta a,.headerMeta button{border:1px solid rgba(255,255,255,.2);background:transparent;color:#fff;border-radius:8px;padding:9px 12px;font-size:10px;font-weight:800;cursor:pointer}.headerMeta form{margin:0}
+.main{max-width:1320px;margin:auto;padding:30px 24px 60px}
+.examHeading{display:flex;align-items:flex-end;justify-content:space-between;gap:30px;margin-bottom:22px}.eyebrow{display:block;color:var(--exam-red);font-size:10px;font-weight:900;letter-spacing:.13em}.examHeading h1{font-size:clamp(31px,5vw,53px);line-height:1;letter-spacing:-.045em;margin:8px 0 9px}.examHeading p{margin:0;color:var(--exam-muted);font-size:12px}
+.timer{flex:0 0 auto;min-width:200px;background:var(--exam-navy);border:1px solid #143750;border-radius:14px;color:#fff;padding:13px 17px;text-align:right;box-shadow:0 14px 34px rgba(7,24,39,.16)}.timer span{display:block;color:#9fb1bf;font-size:8px;font-weight:900;letter-spacing:.1em;text-transform:uppercase}.timer strong{display:block;font-family:ui-monospace,SFMono-Regular,Menlo,monospace;font-size:27px;letter-spacing:-.04em;margin-top:3px}.timerWarning{background:#68131f;border-color:#9d2639}.timerWarning strong{color:#fff}
+.overallProgress{height:9px;background:#dce6ed;border-radius:999px;overflow:hidden}.overallProgress i{display:block;height:100%;background:linear-gradient(90deg,var(--exam-red),#e73854);border-radius:inherit;transition:width .22s}.progressLegend{display:flex;justify-content:space-between;gap:12px;color:var(--exam-muted);font-size:9px;font-weight:800;margin:7px 2px 19px}
+.examLayout{display:grid;grid-template-columns:minmax(0,1fr) 290px;gap:16px;align-items:start}
+.questionCard,.sidebarCard{background:var(--exam-surface);border:1px solid var(--exam-line);box-shadow:0 15px 42px rgba(9,34,52,.075)}
+.questionCard{border-radius:20px;padding:clamp(20px,4vw,34px)}
+.questionTop{display:flex;align-items:flex-start;justify-content:space-between;gap:20px;border-bottom:1px solid var(--exam-line);padding-bottom:17px}.questionTop>div:first-child>span{display:block;font-size:11px;color:var(--exam-red);font-weight:950;letter-spacing:.1em}.questionTop>div:first-child>small{display:block;color:var(--exam-muted);font-size:9px;margin-top:5px}.liveScore{display:grid;grid-template-columns:auto auto;align-items:baseline;column-gap:7px;text-align:right}.liveScore span{grid-column:1/-1;color:var(--exam-muted);font-size:7px;font-weight:900;letter-spacing:.08em}.liveScore strong{font-size:25px;color:var(--exam-navy)}.liveScore small{font-size:10px;color:var(--exam-muted);font-weight:800}
+.questionTags{display:flex;flex-wrap:wrap;gap:7px;margin:17px 0 12px}.questionTags span{border:1px solid #166ba52c;background:#166ba512;color:var(--exam-blue);border-radius:999px;padding:6px 9px;font-size:8px;font-weight:850}.questionCard h2{font-size:clamp(18px,2.4vw,25px);line-height:1.42;letter-spacing:-.018em;margin:0 0 21px}
+.options{display:grid;gap:9px}.option{display:grid;grid-template-columns:38px 1fr;align-items:center;gap:12px;width:100%;border:1px solid var(--exam-line);background:#f8fafc;color:var(--exam-ink);border-radius:13px;padding:12px 14px;text-align:left;line-height:1.48;cursor:pointer;transition:.16s}.option:hover:not(:disabled){border-color:var(--exam-blue);transform:translateY(-1px);box-shadow:0 7px 20px rgba(22,107,165,.08)}.option:disabled{cursor:default;opacity:1}.option b{width:34px;height:34px;border-radius:10px;display:grid;place-items:center;border:1px solid var(--exam-line);background:#fff;color:var(--exam-blue);font-size:12px}.option span{font-size:13px}.optionSelected{border-color:var(--exam-blue);background:#edf7fd;box-shadow:0 0 0 2px rgba(22,107,165,.13)}.optionSelected b{background:var(--exam-blue);border-color:var(--exam-blue);color:#fff}.optionCorrect{border-color:#159466;background:#eaf8f2}.optionCorrect b{background:#087a58;border-color:#087a58;color:#fff}.optionWrong{border-color:#c93b4a;background:#fff0f1}.optionWrong b{background:#b32b37;border-color:#b32b37;color:#fff}
+.feedback{margin-top:16px;border:1px solid var(--exam-line);border-left:4px solid var(--exam-blue);border-radius:13px;padding:15px 16px;background:#f8fafc}.feedbackCorrect{border-left-color:#087a58;background:#f0faf6}.feedbackWrong{border-left-color:#b32b37;background:#fff5f5}.feedback strong{display:block;font-size:13px;margin-bottom:7px}.feedback p{font-size:12px;line-height:1.6;margin:0}.feedback small{display:block;color:var(--exam-muted);font-size:9px;line-height:1.55;margin-top:10px}.saveError{margin-top:13px;border:1px solid #e5aab0;background:#fff1f2;color:#9a2330;border-radius:10px;padding:10px 12px;font-size:11px;font-weight:700}
+.actions{display:flex;align-items:center;justify-content:space-between;gap:12px;border-top:1px solid var(--exam-line);margin-top:20px;padding-top:17px}.actions button{min-height:45px;border-radius:10px;padding:0 17px;font-weight:900;font-size:11px;cursor:pointer}.actions button:disabled{opacity:.52;cursor:default}.stopButton{border:1px solid var(--exam-line);background:#fff;color:var(--exam-muted)}.saveButton,.nextButton{border:1px solid var(--exam-red);background:var(--exam-red);color:#fff;box-shadow:0 10px 24px rgba(200,16,46,.17)}.nextButton{display:inline-flex;align-items:center;gap:13px}.nextButton span{font-size:17px}
+.sidebar{display:grid;gap:12px;position:sticky;top:92px}.sidebarCard{border-radius:16px;padding:16px}.sidebarLabel{display:block;color:var(--exam-muted);font-size:8px;font-weight:950;letter-spacing:.11em}.sidebarSummary{display:grid;grid-template-columns:repeat(3,1fr);gap:7px;margin-top:12px}.sidebarSummary div{background:#f1f5f8;border-radius:9px;padding:9px;text-align:center}.sidebarSummary strong,.sidebarSummary span{display:block}.sidebarSummary strong{font-size:19px}.sidebarSummary span{font-size:7px;color:var(--exam-muted);margin-top:2px}.navigatorHeading{display:flex;align-items:center;justify-content:space-between;gap:12px}.navigatorHeading small{font-size:7px;color:var(--exam-muted)}.questionGrid{display:grid;grid-template-columns:repeat(10,1fr);gap:4px;margin-top:12px}.questionNumber{aspect-ratio:1;border:1px solid var(--exam-line);border-radius:5px;display:grid;place-items:center;background:#f8fafc;color:#81909d;font-size:7px;font-weight:850}.questionAnswered{background:#dff3ea;border-color:#b8decf;color:#087a58}.questionCurrent{background:var(--exam-red);border-color:var(--exam-red);color:#fff;box-shadow:0 4px 10px rgba(200,16,46,.23)}.backLink{display:block;border:1px solid var(--exam-line);border-radius:12px;padding:12px 14px;color:var(--exam-muted);font-size:10px;font-weight:800;text-align:center;background:#f7fafc}.backLink:hover{color:var(--exam-ink);border-color:#b8c7d2}
+.statePage{display:grid;place-content:center;text-align:center;padding:30px}.statePage h1{font-size:34px;letter-spacing:-.035em;margin:17px 0 6px}.statePage p{color:var(--exam-muted);margin:0}.spinner{width:48px;height:48px;margin:auto;border:4px solid #dce5ec;border-top-color:var(--exam-red);border-radius:50%;animation:spin .75s linear infinite}.stateIcon{width:52px;height:52px;margin:auto;border-radius:50%;display:grid;place-items:center;background:#fff0f1;color:var(--exam-red);font-size:27px;font-weight:900}.stateActions{display:flex;justify-content:center;gap:9px;margin-top:20px}.stateActions button,.stateActions a{border:1px solid var(--exam-line);background:#fff;border-radius:9px;padding:11px 14px;font-weight:850;font-size:11px;cursor:pointer}.stateActions button{background:var(--exam-red);border-color:var(--exam-red);color:#fff}
+.resultPage{display:grid;place-items:center;padding:35px 20px}.resultBrand{width:260px;margin-bottom:22px}.resultBrand img{width:100%}.resultCard{width:min(680px,100%);background:#fff;border:1px solid var(--exam-line);border-radius:24px;padding:clamp(25px,5vw,45px);text-align:center;box-shadow:0 22px 65px rgba(7,24,39,.13)}.resultGauge{width:170px;height:170px;border:14px solid var(--exam-navy);border-right-color:var(--exam-red);border-radius:50%;display:grid;place-content:center;margin:18px auto}.resultGauge strong{font-size:41px;letter-spacing:-.05em}.resultGauge span{font-size:7px;color:var(--exam-muted);font-weight:900;letter-spacing:.1em}.resultCard h1{font-size:clamp(28px,5vw,42px);letter-spacing:-.04em;margin:16px 0}.resultCard>p{color:var(--exam-muted);font-size:12px;line-height:1.65}.resultStats{display:grid;grid-template-columns:repeat(3,1fr);gap:9px;margin:20px 0}.resultStats div{background:#f1f5f8;border-radius:11px;padding:13px}.resultStats span,.resultStats strong{display:block}.resultStats span{font-size:7px;color:var(--exam-muted);font-weight:900}.resultStats strong{font-size:24px;margin-top:4px}.successText{color:#087a58}.dangerText{color:var(--exam-red)}.resultActions{display:flex;justify-content:center;gap:9px;margin-top:22px}.resultActions a,.resultActions button{border:1px solid var(--exam-line);border-radius:10px;padding:12px 15px;background:#fff;color:var(--exam-ink);font-size:11px;font-weight:900;cursor:pointer}.resultActions button{background:var(--exam-red);border-color:var(--exam-red);color:#fff}
+.page[data-theme="dark"] .option,.page[data-theme="dark"] .feedback,.page[data-theme="dark"] .stopButton,.page[data-theme="dark"] .backLink,.page[data-theme="dark"] .questionNumber{background:#102433}.page[data-theme="dark"] .option b{background:#0b1b28}.page[data-theme="dark"] .optionSelected{background:#123c57}.page[data-theme="dark"] .optionCorrect,.page[data-theme="dark"] .feedbackCorrect{background:#103a30}.page[data-theme="dark"] .optionWrong,.page[data-theme="dark"] .feedbackWrong{background:#411b24}.page[data-theme="dark"] .sidebarSummary div,.resultPage[data-theme="dark"] .resultStats div{background:#102433}.resultPage[data-theme="dark"] .resultCard{background:var(--exam-surface)}.resultPage[data-theme="dark"] .resultActions a,.statePage[data-theme="dark"] .stateActions a{background:#102433;color:var(--exam-ink)}
+@keyframes spin{to{transform:rotate(360deg)}}
+
+@media(max-width:980px){.examLayout{grid-template-columns:1fr}.sidebar{position:static;grid-template-columns:1fr 1fr}.backLink{grid-column:1/-1}.questionGrid{grid-template-columns:repeat(20,1fr)}}
+@media(max-width:700px){.header{height:68px}.headerInner{padding:0 13px}.brand{width:185px}.headerMeta>span,.headerMeta a{display:none}.headerMeta button{padding:8px 10px}.main{padding:22px 12px 45px}.examHeading{align-items:stretch;flex-direction:column;gap:16px}.timer{text-align:left;min-width:0;display:flex;align-items:center;justify-content:space-between}.timer strong{font-size:23px}.examLayout{gap:11px}.questionCard{border-radius:16px;padding:18px 14px}.questionCard h2{font-size:18px}.option{grid-template-columns:34px 1fr;padding:11px 10px}.option span{font-size:12px}.actions{align-items:stretch;flex-direction:column-reverse}.actions button{width:100%}.nextButton{justify-content:center}.sidebar{grid-template-columns:1fr}.backLink{grid-column:auto}.questionGrid{grid-template-columns:repeat(10,1fr)}.resultActions,.stateActions{flex-direction:column}.resultActions>*{width:100%}.resultBrand{width:210px}.resultStats{gap:5px}.resultStats div{padding:10px 6px}}
