import {readFile,stat} from "node:fs/promises";import path from "node:path";import {getAccessContext} from "../../../lib/access";export const dynamic="force-dynamic";export const runtime="nodejs";const root=path.join(process.cwd(),'protected-content','study-content');const mime={'.css':'text/css; charset=utf-8','.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.json':'application/json; charset=utf-8','.png':'image/png','.jpg':'image/jpeg','.jpeg':'image/jpeg','.svg':'image/svg+xml','.webp':'image/webp','.pdf':'application/pdf','.woff':'font/woff','.woff2':'font/woff2'};const H=`<style>body{padding-top:76px!important}.estibordo-fixed{position:fixed;z-index:2147483647;left:0;right:0;top:0;height:76px;background:#081522;border-bottom:1px solid rgba(255,255,255,.1);font-family:Arial,sans-serif}.estibordo-fixed>div{max-width:1240px;height:100%;margin:auto;padding:0 18px;display:flex;align-items:center;justify-content:space-between}.estibordo-fixed img{width:220px;max-height:55px}.estibordo-fixed nav{display:flex;gap:7px}.estibordo-fixed a{color:white!important;text-decoration:none!important;border:1px solid rgba(255,255,255,.2);padding:8px 10px;border-radius:8px;font-size:11px;font-weight:800}</style><header class="estibordo-fixed"><div><a href="/" style="border:0"><img src="/estibordo/logos/estibordo-logo-header.png"></a><nav><a href="/area-do-aluno">Painel</a><a href="/conteudos">Conteúdos</a><a href="/simulado">Simulados</a><a href="/perfil">Perfil</a></nav></div></header>`;function deny(req,s,a){if(!s)return Response.redirect(new URL('/login',req.url),303);const u=new URL('/comprar',req.url);u.searchParams.set('locked',a?.effective_status==='expired'?'expired':'inactive');return Response.redirect(u,303)}export async function GET(req,{params}){const {session,access,active}=await getAccessContext();if(!active)return deny(req,session,access);const seg=(await params).path||[],requested=path.resolve(root,...seg);if(requested!==root&&!requested.startsWith(root+path.sep))return new Response('Não encontrado.',{status:404});try{const info=await stat(requested),file=info.isDirectory()?path.join(requested,'index.html'):requested,fi=info.isDirectory()?await stat(file):info;if(!fi.isFile())throw Object.assign(new Error(),{code:'ENOENT'});const ext=path.extname(file).toLowerCase();let b=await readFile(file);if(ext==='.html'){let s=b.toString('utf8');s=/<body[^>]*>/i.test(s)?s.replace(/<body([^>]*)>/i,`<body$1>${H}`):H+s;b=Buffer.from(s)}return new Response(b,{headers:{'Cache-Control':'private, no-store','Content-Type':mime[ext]||'application/octet-stream','X-Content-Type-Options':'nosniff'}})}catch{return new Response('Não encontrado.',{status:404})}}
