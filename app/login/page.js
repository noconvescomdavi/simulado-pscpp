"use client";
import {useState} from "react";
import {Nav,Footer} from "../components";
import TurnstileWidget from "../components/TurnstileWidget";
export default function Login(){
 const [msg,setMsg]=useState(""),[showPassword,setShowPassword]=useState(false);
 async function submit(e){e.preventDefault();setMsg("Entrando...");const f=new FormData(e.currentTarget),o=Object.fromEntries(f);const r=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email:o.email,password:o.password,turnstile_token:o["cf-turnstile-response"]||""})});const j=await r.json();if(r.ok){location.href=j.requiresAdminMfa?"/mfa-admin":j.requiresMfa?"/mfa":j.requiresTerms?"/aceitar-termos":j.requiresMfaSetup?"/configurar-2fa":"/area-do-aluno"}else if(j.code==="EMAIL_NOT_VERIFIED")location.href="/verificar-email?email="+encodeURIComponent(j.email||o.email||"");else setMsg(j.error||"Não foi possível entrar. Verifique seus dados.");}
 return <><Nav/><main className="authPage"><section className="authShell"><div className="authStory"><span>ÁREA DO ALUNO</span><h1>Continue sua preparação de onde parou.</h1><p>Use senha ou sua conta Google. Se você ativou 2FA, o segundo fator será solicitado antes de abrir a plataforma.</p><ul><li>Sessão protegida</li><li>Google OAuth</li><li>2FA TOTP opcional</li><li>Proteção anti-bot</li></ul></div><div className="authForm"><div className="eyebrow">BEM-VINDO DE VOLTA</div><h2>Entrar</h2><a className="googleAuthButton" href="/api/auth/google/start?intent=login"><span>G</span> Entrar com Google</a><div className="authDivider"><span>ou use sua senha</span></div><form onSubmit={submit}><div className="field"><label>E-MAIL</label><input name="email" type="email" autoComplete="email" required/></div><div className="field"><label>SENHA</label><div className="passwordWrap"><input name="password" type={showPassword?"text":"password"} autoComplete="current-password" required/><button type="button" className="passwordToggle" onClick={()=>setShowPassword(v=>!v)}>{showPassword?"Ocultar":"Mostrar"}</button></div></div><div style={{textAlign:"right",marginTop:"-6px",marginBottom:"12px"}}><a href="/esqueci-minha-senha" style={{fontSize:11,fontWeight:800,color:"#c8102e"}}>Esqueci minha senha</a></div><TurnstileWidget/><button className="btn primary full">Entrar na plataforma</button><div className="msg" role="status">{msg}</div></form><div className="authFoot">Ainda não possui conta? <a href="/cadastro">Criar minha conta</a></div></div></section></main><Footer/></>
}