"use client";
import {useState} from "react";
export default function Login(){const [msg,setMsg]=useState("");async function submit(e){e.preventDefault();setMsg("Entrando...");const f=new FormData(e.currentTarget);const r=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(f))});const j=await r.json();if(r.ok)location.href="/area-do-aluno";else setMsg(j.error||"Falha no login");}return <main className="wrap"><section className="auth"><div className="eyebrow">ÁREA DO ALUNO</div><h1>Entrar</h1><form onSubmit={submit}><div className="field"><label>E-MAIL</label><input name="email" type="email" required/></div><div className="field"><label>SENHA</label><input name="password" type="password" required/></div><button className="btn primary full">Entrar</button><div className="msg">{msg}</div></form><p className="muted">Ainda não possui conta? <a href="/cadastro"><b>Criar conta</b></a></p></section></main>}