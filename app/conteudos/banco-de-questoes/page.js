import {redirect} from "next/navigation";import {getSession} from "../../../lib/auth";import {getUserAccess} from "../../../lib/access";import {availableQuestionBanks} from "../../../lib/question-banks";import StudentHeader from "../../components/StudentHeader";import Builder from "./Builder";import styles from "./bank.module.css";export default async function Page(){const s=await getSession();if(!s)redirect('/login');if(!(await getUserAccess(s.id))?.active)redirect('/comprar');return <><StudentHeader active="conteudos"/><main className={styles.page}><span>QUESTÕES</span><h1>Banco de questões</h1><p>Marque as matérias e gere um caderno aleatório de 1 a 100 questões.</p><Builder banks={availableQuestionBanks()}/></main></>}