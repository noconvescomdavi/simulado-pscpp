"use client";
import {useEffect,useState} from "react";import StudentHeader from "../../components/StudentHeader";import {availableQuestionBanks} from "../../../lib/question-banks";import {listOfflineNotebooks} from "../../../lib/offline-store";import Builder from "./Builder";import styles from "./bank.module.css";
export default function StandaloneBank({requested="",fixation=null}){const [history,setHistory]=useState([]),banks=availableQuestionBanks({includeFilters:true}),initialSubjects=requested&&banks.some(b=>b.slug===requested&&b.count)?[requested]:[];useEffect(()=>{listOfflineNotebooks().then(setHistory).catch(()=>setHistory([]))},[]);return <><StudentHeader active="banco"/><main className={styles.page}><span>QUESTÕES</span><h1>Banco de questões</h1><p>Gere cadernos usando somente o conteúdo armazenado no dispositivo.</p><Builder banks={banks} trial={false} initialSubjects={initialSubjects} fixation={fixation}/><section id="meus-cadernos" className={styles.history}><div className={styles.historyHead}><div><span>HISTÓRICO LOCAL</span><h2>Meus cadernos</h2><p>Cadernos e respostas persistem no dispositivo.</p></div><strong>{history.length} {history.length===1?"caderno":"cadernos"}</strong></div>{history.length?<div className={styles.historyList}>{history.map(x=>{const answered=Object.keys(x.answers||{}).length,correct=Object.values(x.answers||{}).filter(a=>a.is_correct).length,score=answered?correct/answered*100:0;return <article className={styles.historyCard} key={x.id}><div className={styles.historyMain}><h3>{x.title}</h3><p>{answered} de {x.total_questions} questões respondidas</p><div className={styles.progressTrack}><i style={{width:(x.total_questions?Math.round(answered/x.total_questions*100):0)+"%"}}/></div></div><div className={styles.historyStats}><div><span>Aproveitamento</span><strong>{score.toFixed(0)}%</strong></div><a href={"/offline?mode=notebook&id="+x.id}>Abrir caderno →</a></div></article>})}</div>:<div className={styles.historyEmpty}><strong>Nenhum caderno criado ainda.</strong></div>}</section></main></>}
