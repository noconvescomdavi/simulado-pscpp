import {redirect,notFound} from "next/navigation";import {getSession} from "../../../../lib/auth";import {getUserAccess} from "../../../../lib/access";import {getNotebook} from "../../../../lib/notebooks";import StudentHeader from "../../../components/StudentHeader";import Client from "./Client";export default async function Page({params}){const s=await getSession();if(!s)redirect('/login');if(!(await getUserAccess(s.id))?.active)redirect('/comprar');const {id}=await params,n=await getNotebook(s.id,id);if(!n)notFound();return <><StudentHeader active="conteudos"/><Client notebook={n}/></>}