"use client";import {useEffect,useState} from "react";import StudentHeader from "../../components/StandaloneStudentHeader";import StructuredQuestion from "../../components/StructuredQuestion";import {STANDALONE_STORES} from "../../../lib/standalone/schema";import {standaloneAll} from "../../../lib/standalone/store";import styles from "./errors.module.css";export default function StandaloneErrors(){const [items,setItems]=useState([]);useEffect(()=>{standaloneAll(STANDALONE_STORES.answers).then(rows=>{const map=new Map();for(const x of rows.filter(a=>a.is_correct===false)){const k=String(x.question_id||x.id);const v=map.get(k)||{...x,errors:0};v.errors++;map.set(k,v)}setItems([...map.values()].sort((a,b)=>b.errors-a.errors))})},[]);return <><StudentHeader active="erros"/><main className={styles.page}><span>REVISÃO LOCAL</span><h1>Caderno de erros</h1><p className={styles.intro}>Questões erradas registradas neste dispositivo.</p><div className={styles.summary}><div><b>{items.length}</b><span>questões para revisar</span></div><div><b>{items.reduce((s,x)=>s+x.errors,0)}</b><span>erros acumulados</span></div></div><section className={styles.list}>{items.length?items.map(x=><article className={styles.card} key={x.question_id||x.id}><div className={styles.top}><div><span>{x.subject_label||x.subject||"Questão"}</span><h2>{x.topic||"Revisão"}</h2></div><div className={styles.metrics}><b>{x.errors} erros</b></div></div>{x.question&&<div className={styles.question}><StructuredQuestion question={{question:x.question}}/></div>}<div className={styles.attemptContext}><span><b>Última resposta:</b> {x.selected_answer||"—"}</span><span><b>Resposta correta:</b> {x.correct_answer||"—"}</span></div><div className={styles.actions}><a href={"/conteudos/banco-de-questoes?subject="+encodeURIComponent(x.subject||"")}>Treinar esta matéria →</a></div></article>):<div className={styles.empty}>Nenhum erro registrado ainda.</div>}</section></main></>}