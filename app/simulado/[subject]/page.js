import {redirect,notFound} from "next/navigation";import {getSession} from "../../../lib/auth";import {getUserAccess} from "../../../lib/access";import {getQuestionBank} from "../../../lib/question-banks";import {subjectLabel} from "../../../lib/subjects";import StudentHeader from "../../components/StudentHeader";import Client from "./Client";export default async function Page({params}){const s=await getSession();if(!s)redirect('/login');if(!(await getUserAccess(s.id))?.active)redirect('/comprar');const {subject}=await params,b=getQuestionBank(subject);if(!b)notFound();return <><StudentHeader active="simulados"/><Client subject={subject} title={subjectLabel(subject)} ready={(b.questions||[]).length>0}/></>}