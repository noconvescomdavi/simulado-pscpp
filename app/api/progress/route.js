import {getSession} from "../../../lib/auth";import {query} from "../../../lib/db";
export async function GET(){const s=await getSession();if(!s)return Response.json({error:"Não autenticado"},{status:401});const r=await query("select subject,percent,completed_items,total_items,updated_at from study_progress where user_id=$1 order by subject",[s.id]);return Response.json({progress:r.rows})}
export async function POST(req){const s=await getSession();if(!s)return Response.json({error:"Não autenticado"},{status:401});const b=await req.json();const percent=Math.max(0,Math.min(100,Number(b.percent)||0));await query(`insert into study_progress(user_id,subject,percent,completed_items,total_items) values($1,$2,$3,$4,$5) on conflict(user_id,subject) do update set percent=excluded.percent,completed_items=excluded.completed_items,total_items=excluded.total_items,updated_at=now()`,[s.id,String(b.subject),percent,Number(b.completed_items)||0,Number(b.total_items)||0]);return Response.json({ok:true})}