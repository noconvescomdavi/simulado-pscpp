import {getSession} from "../../../../../lib/auth";import {getUserAccess} from "../../../../../lib/access";import {answerNotebook} from "../../../../../lib/notebooks";export async function POST(r,{params}){const s=await getSession();if(!s)return Response.json({error:'Não autenticado.'},{status:401});if(!(await getUserAccess(s.id))?.active)return Response.json({error:'Acesso inativo.'},{status:403});const {id}=await params,b=await r.json().catch(()=>({}));const x=await answerNotebook({userId:s.id,notebookId:id,subject:b.subject,questionId:b.question_id,selectedAnswer:b.selected_answer});return Response.json(x,{status:x.status||200})}