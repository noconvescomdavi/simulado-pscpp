import {notFound} from "next/navigation";import design from "../../../data/site/editor-design.json";export function generateMetadata({params}){const route="/paginas/"+params?.slug;const page=design?.pages?.[route];return {title:page?.settings?.seoTitle?page.settings.seoTitle+" | ESTIBORDO":"Página | ESTIBORDO",description:page?.settings?.seoDescription||"Página criada no editor visual ESTIBORDO"}}export default function ManagedPage({params}){const route="/paginas/"+params.slug;if(!design?.pages?.[route])notFound();return <main data-estibordo-managed-page={route} style={{minHeight:"60vh"}}/>}