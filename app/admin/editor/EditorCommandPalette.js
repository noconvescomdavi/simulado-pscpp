"use client";import {useEffect,useMemo,useState} from "react";export default function EditorCommandPalette({open,onClose,commands}){const [q,setQ]=useState("");useEffect(()=>{if(open)setQ("")},[open]);const list=useMemo(()=>commands.filter(([n])=>n.toLowerCase().includes(q.toLowerCase())),[commands,q]);if(!open)return null;return <div className="ev-command-backdrop" onMouseDown={e=>e.target===e.currentTarget&&onClose()}><div className="ev-command"><input autoFocus value={q} onChange={e=>setQ(e.target.value)} placeholder="Digite um comando…"/><div>{list.map(([name,fn])=><button key={name} onClick={()=>{fn();onClose()}}>{name}</button>)}</div><small>Ctrl+K · Command Palette</small></div></div>}