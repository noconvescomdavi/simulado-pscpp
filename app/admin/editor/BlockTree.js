"use client";function label(block){return block.title||block.text||block.quote||block.placeholder||block.type}function Node({block,depth,selectedId,onSelect}){return <div><button type="button" className={"ev-block-node "+(selectedId===block.id?"is-active":"")} style={{paddingLeft:7+depth*7}} onClick={()=>onSelect(block.id)}><i>{(block.children||[]).length?"▾":"·"}</i><small>{block.type}</small><span>{label(block)}</span></button>{(block.children||[]).length>0&&<div className="ev-block-children">{block.children.map(child=><Node key={child.id} block={child} depth={depth+1} selectedId={selectedId} onSelect={onSelect}/>)}</div>}</div>}export default function BlockTree({blocks,selectedId,onSelect}){if(!(blocks||[]).length)return <div className="ev-block-tree"><p className="ev-drawer-empty">Nenhum bloco criado pelo editor nesta página.</p></div>;return <div className="ev-block-tree">{blocks.map(block=><Node key={block.id} block={block} depth={0} selectedId={selectedId} onSelect={onSelect}/>)}</div>}