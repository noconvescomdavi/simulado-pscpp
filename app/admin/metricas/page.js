import {query} from "../../../lib/db";export const dynamic="force-dynamic";export default async function Page(){const x=(await query(`select count(*)::int total,count(*) filter(where is_correct)::int correct from question_answers`)).rows[0]||{};return <main className="wrap admin-wrap"><h1>Métricas</h1><section className="metrics"><div className="metric"><span>RESPOSTAS</span><b>{x.total||0}</b></div><div className="metric"><span>ACERTOS</span><b>{x.correct||0}</b></div><div className="metric"><span>APROVEITAMENTO</span><b>{x.total?Math.round(100*x.correct/x.total):0}%</b></div></section></main>}