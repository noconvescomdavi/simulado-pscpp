import {availableQuestionBanks} from "../../../lib/question-banks";
export default function Page(){const banks=availableQuestionBanks();const total=banks.reduce((n,x)=>n+Number(x.count||0),0);return <main className="wrap admin-wrap"><div className="eyebrow">ADMINISTRAÇÃO</div><h1>Bancos de Questões</h1><p>{banks.length} bancos carregados, totalizando {total.toLocaleString("pt-BR")} questões disponíveis para a plataforma.</p><div className="grid">{banks.map(x=><article className="card" key={x.slug}><span className="status status-active">ATIVO</span><h3>{x.title}</h3><p><strong>{Number(x.count||0).toLocaleString("pt-BR")}</strong> questões</p><code>data/questions/{x.slug}.json</code></article>)}</div></main>}