"use client";import {useEffect,useState} from "react";import StudentHeader from "../components/StandaloneStudentHeader";import {STANDALONE_STORES} from "../../lib/standalone/schema";import {standaloneAll} from "../../lib/standalone/store";import {getLocalLearningMetrics} from "../../lib/standalone/analytics";import styles from "../treino-adaptativo/adaptive.module.css";export default function StandaloneTraining(){const [d,setD]=useState({subjects:[],reviews:[]});useEffect(()=>{Promise.all([getLocalLearningMetrics(),standaloneAll(STANDALONE_STORES.reviewQueue)]).then(([m,r])=>setD({subjects:[...m.subjects].sort((a,b)=>a.accuracy-b.accuracy),reviews:r.filter(x=>!x.due_at||new Date(x.due_at)<=new Date()).slice(0,20)}))},[]);return <><StudentHeader active="adaptativo"/><main className={styles.page}><section className={styles.hero}><span>TREINO INTELIGENTE</span><h1>Prioridades e revisões calculadas localmente.</h1><p>O aplicativo usa seu histórico armazenado no aparelho para priorizar matérias e revisões.</p></section><section className={styles.grid}><article className={styles.panel}><span>PRIORIDADES</span><h2>O que treinar agora</h2><div className={styles.list}>{d.subjects.map((x,i)=><div key={x.slug}><b>{i+1}</b><div><strong>{x.label}</strong><small>{x.questions} respostas · {x.errors} erros</small></div><em>{Math.round(x.accuracy)}%</em></div>)}</div></article><article className={styles.panel} id="revisoes"><span>REVISÕES</span><h2>{d.reviews.length} pendentes</h2><div className={styles.list}>{d.reviews.map(x=><div key={x.id}><div><strong>{x.topic||x.source_key||"Revisão"}</strong><small>{x.subject_label||x.subject_slug||""}</small></div></div>)}{!d.reviews.length&&<p>Nenhuma revisão vencida agora.</p>}</div></article></section></main></>}