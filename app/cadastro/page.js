"use client";
import {useState} from "react";
export default function Cadastro(){const [msg,setMsg]=useState("");async function submit(e){e.preventDefault();const f=new FormData(e.currentTarget);const o=Object.fromEntries(f);if(o.password!==o.confirm){setMsg("As senhas não coincidem.");return}setMsg("Criando conta...");const r=await fetch("/api/auth/register",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email:o.email,password:o.password})});const j=await r.json();if(r.ok)location.href="/plano-de-estudos/configurar";else setMsg(j.error||"Falha no cadastro");}return <main className="wrap"><section className="auth"><div className="eyebrow">NOVO ALUNO</div><h1>Criar conta</h1><p className="muted">A plataforma solicita somente e-mail e senha para a conta. A senha é armazenada exclusivamente como hash Argon2id.</p><form onSubmit={submit}><div className="field"><label>E-MAIL</label><input name="email" type="email" required/></div><div className="field"><label>SENHA</label><input name="password" type="password" minLength="10" required/></div><div className="field"><label>CONFIRMAR SENHA</label><input name="confirm" type="password" minLength="10" required/></div><button className="btn primary full">Criar conta</button><div className="msg">{msg}</div></form></section></main>}