import {getPscppPool} from "../lib/pscpp-exam-bank.js";import {buildPscppFixedExam,historicalSubject,PSCPP_FIXED_QUOTAS} from "../lib/historical-exam-blueprint.js";const exam=buildPscppFixedExam(getPscppPool(),{seed:"android-local-runtime"});const counts={};for(const q of exam)counts[historicalSubject(q)]=(counts[historicalSubject(q)]||0)+1;if(exam.length!==100)throw new Error("Expected 100, got "+exam.length);for(const [s,n] of Object.entries(PSCPP_FIXED_QUOTAS))if(counts[s]!==n)throw new Error(s+": expected "+n+", got "+counts[s]);console.log(JSON.stringify({total:exam.length,counts},null,2));
