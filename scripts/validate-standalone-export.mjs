import fs from "node:fs/promises";const file=process.argv[2];if(!file)throw new Error("Uso: node scripts/validate-standalone-export.mjs <arquivo.json>");const x=JSON.parse(await fs.readFile(file,"utf8"));if(x.format!=="estibordo-standalone-progress"||x.export_version!==1||!Array.isArray(x.changes))throw new Error("Export standalone inválido.");const forbidden=/(password|hash|token|secret|mfa|oauth|database_url|session_cookie)/i;function walk(v,path="root"){if(Array.isArray(v))return v.forEach((x,i)=>walk(x,path+"["+i+"]"));if(v&&typeof v==="object")for(const [k,x] of Object.entries(v)){if(forbidden.test(k))throw new Error("Campo sensível encontrado: "+path+"."+k);walk(x,path+"."+k)}}walk(x);console.log("Export standalone válido:",x.mode,x.changes.length,"alterações");
