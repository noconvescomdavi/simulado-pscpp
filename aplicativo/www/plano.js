const root=document.getElementById("app");function esc(s){return String(s??"").replace(/[&<>"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[c]))}async function boot(){const tasks=await EstibordoLocal.initPlan();root.innerHTML=`<main class="shell"><a href="./index.html">← Início</a><p class="eyebrow">PLANO DE ESTUDOS</p><h1>Plano de Estudos</h1><section class="grid">${tasks.map(t=>`<button class="card" data-id="${esc(t.id)}"><b>${t.done?"✓ ":""}${esc(t.title)}</b><span>${t.done?"Concluída":"Pendente"} · meta ${t.target||20} questões</span></button>`).join("")}</section></main>`;root.querySelectorAll("[data-id]").forEach(b=>b.onclick=async()=>{await EstibordoLocal.toggleTask(b.dataset.id);boot()})}boot();